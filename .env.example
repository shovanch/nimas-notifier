# Where to read the *page* (only used for Playwright or for links in alerts)
TARGET_URL=

# JSON endpoint (fast path)
NIMAS_API_URL=

# Which row to watch (matches "Serial No" in the API)
ROW_MATCH=

# Alert when availability is strictly less than this
THRESHOLD=

# Choose backend: api | playwright | auto
SCRAPER_BACKEND=auto

# Telegram creds (required)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
